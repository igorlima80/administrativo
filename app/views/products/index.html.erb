<%= render 'shared/index', list: @products, box_class: 'danger',
  sort_all: true,
  search_fields: [
    { field: 'name_cont', label: 'Nome' },
    { field: 'category_id_eq', label: 'Categoria', collection: Category.all.order(name: :asc) }
  ]
%>
