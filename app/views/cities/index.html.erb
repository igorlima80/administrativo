<%= render 'shared/index', list: @cities, box_class: 'danger',
  sort_fields: [
    'name', 'ibgeid', 'state_id'
  ],
  search_fields: [
    { field: 'name_cont', label: 'Nome' },
    { field: 'state_id_eq', label: 'Estado', collection: State.all.order(:name) }
  ]
%>
